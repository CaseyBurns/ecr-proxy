# ecr-proxy

![CI](https://github.com/CaseyBurns/ecr-proxy/workflows/CI/badge.svg)

## tasks

ensure invoke is installed:

```powershell
pip install invoke
```

list build tasks:

```powershell
invoke --list
```
